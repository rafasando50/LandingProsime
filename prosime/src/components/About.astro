<section id="nosotros" class="about">
	<div class="about-container">
		<div class="about-grid grid">
			<div class="about-content fade-in">
				<div class="section-label">Identidad Corporativa</div>
				<h2 class="section-title">Compromiso <br /><span class="gradient-text">con la Excelencia</span></h2>
				
				<div class="about-text">
					<p>
						Nuestro <strong>propósito</strong> es consolidarnos como lideres en el mercado mexicano, proveyendo soluciones especializadas en medición y control de fluidos para los sectores de petróleo y gas.
					</p>
					<p>
						Nuestra <strong>misión</strong> es apoyar a nuestros clientes en el cumplimiento de sus procesos industriales, suministrando equipos de alta eficiencia para garantizar la productividad y seguridad.
					</p>
					<p>
						Nuestra <strong>visión</strong> es ser el referente lider en proyectos industrales EPC, impulsando la industria con infraestructura propia, innovación constante y los más altos estandares.
					</p>
				</div>

				<div class="stats-row">
					<div class="stat-card light-card">
						<div class="stat-value">ASME</div>
						<div class="stat-name">Certificación</div>
					</div>
					<div class="stat-card light-card">
						<div class="stat-value">+10</div>
						<div class="stat-name">Años Sector</div>
					</div>
				</div>
			</div>

			<div class="about-visual fade-in">
				<div class="image-stack">
					<div class="main-image">
						<img src="/asme-vessel.png" alt="ASME Vessel Manufacturing" loading="lazy" />
					</div>
					<div class="accent-box"></div>
					<div class="floating-badge light-card">
						<span class="highlight">100%</span>
						<p>Calidad Certificada</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	/* --- Estilos Base (Desktop First) --- */
	.about {
		background: hsl(var(--bg-primary));
		padding: 8rem 2rem;
		position: relative;
		overflow: hidden;
	}

	.about-container {
		max-width: 1400px;
		margin: 0 auto;
	}

	.about-grid {
		display: grid;
		grid-template-columns: 1.2fr 0.8fr;
		gap: 6rem;
		align-items: center;
	}

	.section-label {
		font-size: 0.75rem;
		font-weight: 800;
		text-transform: uppercase;
		letter-spacing: 0.2rem;
		color: hsl(var(--text-muted));
		margin-bottom: 1rem;
	}

	.section-title {
		font-size: clamp(2.5rem, 5vw, 4rem); /* Tamaño fluido */
		line-height: 1.1;
		margin-bottom: 2rem;
	}

	.about-text {
		color: hsl(var(--text-muted));
		font-size: 1.1rem;
		line-height: 1.6;
		margin-bottom: 3rem;
		max-width: 600px;
	}

	.about-text strong {
		color: hsl(var(--navy-primary));
	}

	.stats-row {
		display: flex;
		flex-wrap: wrap; /* Permite que bajen en móviles muy pequeños */
		gap: 2rem;
	}

	.stat-card {
		padding: 1.5rem 2rem;
		flex: 1; /* Crecen equitativamente */
		min-width: 180px;
		text-align: center;
		background: white; /* Ajuste si light-card no carga */
		border-radius: 8px;
	}

	.stat-value {
		font-family: 'Outfit', sans-serif;
		font-size: 2.5rem;
		font-weight: 800;
		color: hsl(var(--navy-primary));
		margin-bottom: 4px;
	}

	.stat-name {
		font-size: 0.75rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.1rem;
		color: hsl(var(--text-muted));
	}

	/* --- Visual / Imágenes --- */
	.about-visual {
		position: relative;
		padding-right: 2rem;
	}

	.image-stack {
		position: relative;
		z-index: 10;
	}

	.main-image img {
		width: 100%;
		height: auto;
		border-radius: 12px;
		box-shadow: 0 40px 80px -40px rgba(10, 25, 47, 0.2);
		display: block;
	}

	.accent-box {
		position: absolute;
		top: -20px;
		right: -20px;
		width: 100px;
		height: 100px;
		border-top: 4px solid hsl(var(--yellow-accent, #ffc107));
		border-right: 4px solid hsl(var(--yellow-accent, #ffc107));
		z-index: -1;
	}

	.floating-badge {
		position: absolute;
		bottom: 30px;
		left: -40px;
		padding: 1.2rem;
		z-index: 10;
		min-width: 160px;
		background: #ffffff !important;
		border-left: 4px solid hsl(var(--yellow-accent));
		box-shadow: 0 10px 30px rgba(0,0,0,0.1);
	}

	.floating-badge .highlight {
		font-size: 1.8rem;
		font-weight: 800;
		color: hsl(var(--navy-primary));
		display: block;
	}

	.floating-badge p {
		font-size: 0.7rem;
		font-weight: 700;
		text-transform: uppercase;
		margin: 0;
		color: hsl(var(--text-muted));
	}

	@media (max-width: 1440px) {
		.about-text {
			margin-left: 3%;
		}
		.section-title {
			margin-left: 3%;
		}
		.section-label {
			margin-left: 3%;
		}
	}

	@media (max-width: 1024px) {
		.about-grid {
			gap: 3rem;
		}
		.about {
			padding: 6rem 1.5rem;
		}
		.about-text {
			margin-left: 2%;
		}
		.section-title {
			margin-left: 2%;
		}
		.section-label {
			margin-left: 2%;
		}
	}

	@media (max-width: 992px) {
		.about-grid {
			grid-template-columns: 1fr; /* Columna única */
			text-align: center;
		}

		.about-text {
			margin-left: auto;
			margin-right: auto;
		}

		.stats-row {
			justify-content: center;
		}

		.about-visual {
			max-width: 600px;
			margin: 3rem auto 0 auto;
			padding-right: 0;
		}

		.floating-badge {
			left: 0;
			bottom: -20px;
		}
		
		.accent-box {
			top: -15px;
			right: -15px;
		}
	}

	@media (max-width: 425px) {
		.about {
			width: 350px;
			margin-left:12%;
		}
		.stat-card {
			margin-top: -10%;
		}
		.stats-row {
			margin-bottom: -10%;
		}
	}	

	@media (max-width: 375px)  {
		.about-container {
			width: 100% !important;
			padding: 0 1rem;
		}
		.about {
			width: 100%;
			margin-left: 0;
		}
		.stat-card {
			margin-top: -10%;
		}
		.stats-row {
			margin-bottom: -10%;
		}
	}

	@media (max-width: 320px) {
		.about {
			width: 100%;
			margin-left: 0;
		}
		.stat-card {
			margin-top: -10%;
		}
		.stats-row {
			margin-bottom: -10%;
		}
	}

</style>