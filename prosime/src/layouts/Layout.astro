<!doctype html>
<html lang="es">
	<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	<link rel="icon" href="/favicon.ico?v=2" type="image/x-icon" />

	<meta name="generator" content={Astro.generator} />
	<title>Prosime Industrial | Proyectos y Soluciones de Ingeniería</title>

	<!-- Google Fonts: Inter & Outfit -->
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;600;700&display=swap" rel="stylesheet">
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		/* Light Theme Palette (HSL / Hex) */
		--bg-primary: 0 0% 100%;    /* Pure White */
		--bg-secondary: 210 40% 98%; /* Very Light Gray/Slate */
		
		--navy-primary: 247 54% 33%;   /* Brand Color: #312783 */
		--navy-medium: 247 54% 25%;
		--navy-light: 247 54% 45%;
		
		--yellow-accent: 48 96% 53%;  /* Highlights: Vivid Yellow */
		--red-accent: 0 84% 60%;     /* Alert/Logo: Vibrant Red */
		
		--text-main: 222 47% 8%;     /* Deep Navy for text */
		--text-muted: 215 20% 45%;   /* Muted Slate */
		
		--glass-bg: 0 0% 100% / 0.8;
		--glass-border: 222 47% 8% / 0.1;
		
		--transition: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
		--shadow-soft: 0 10px 30px -10px rgba(10, 25, 47, 0.1);
	}

	html {
		scroll-behavior: smooth;
		background-color: hsl(var(--bg-primary));
	}

	body {
		margin: 0;
		padding: 0;
		color: hsl(var(--text-main));
		font-family: 'Inter', system-ui, sans-serif;
		line-height: 1.6;
		overflow-x: hidden;
		-webkit-font-smoothing: antialiased;
		background-color: hsl(var(--bg-primary));
	}

	/* Optional: Keep a very subtle texture for premium feel */
	body::before {
		content: "";
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		opacity: 0.02;
		z-index: 9999;
		pointer-events: none;
		background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
	}

	h1, h2, h3 {
		font-family: 'Outfit', sans-serif;
		font-weight: 700;
		letter-spacing: -0.02em;
		color: hsl(var(--navy-primary));
	}

	.section-title {
		font-size: clamp(2.5rem, 5vw, 4rem);
		line-height: 1.1;
		margin-bottom: 1.5rem;
	}

	.gradient-text {
		background: linear-gradient(135deg, hsl(var(--navy-primary)), hsl(var(--navy-light)));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}

	.accent-text {
		color: hsl(var(--yellow-accent));
	}

	.btn {
		position: relative;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 1rem 2rem;
		font-weight: 800; /* Extra bold as requested in previous steps */
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 0.875rem;
		border-radius: 4px; /* Reduced to 4px for a more industrial look */
		transition: var(--transition);
		cursor: pointer;
		overflow: hidden;
		text-decoration: none; /* No underline */
	}

	.btn-primary {
		background: hsl(var(--navy-primary));
		color: #ffffff;
		box-shadow: 0 10px 30px -10px hsla(var(--navy-primary), 0.3);
	}

	.btn-primary:hover {
		transform: translateY(-2px);
		background: hsl(var(--navy-medium));
		box-shadow: 0 15px 40px -10px hsla(var(--navy-primary), 0.5);
		text-decoration: none;
	}

	.btn-outline {
		border: 1px solid hsl(var(--navy-primary));
		background: transparent;
		color: hsl(var(--navy-primary));
		text-decoration: none;
	}

	.btn-outline:hover {
		background: hsla(var(--navy-primary), 0.05);
		text-decoration: none;
	}

	/* Card Base for Light Theme */
	.light-card {
		background: #ffffff;
		border: 1px solid hsla(var(--navy-primary), 0.1);
		border-radius: 12px;
		transition: var(--transition);
		box-shadow: var(--shadow-soft);
	}

	.light-card:hover {
		border-color: hsla(var(--yellow-accent), 0.5);
		box-shadow: 0 20px 40px -20px rgba(10, 25, 47, 0.15);
	}

	::selection {
		background: hsl(var(--yellow-accent));
		color: #ffffff;
	}
</style>
